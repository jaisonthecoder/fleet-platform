# Do NOT let pnpm self-download/switch versions from the "packageManager" field:
# the self-managed binary cache is broken on this machine (fails to fetch
# @pnpm/exe). Install the pinned pnpm version directly instead (`pnpm self-update`).
manage-package-manager-versions=false
