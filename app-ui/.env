# Shared, non-secret defaults loaded for every mode.
# WARNING: every VITE_* variable is embedded into the client bundle and is PUBLIC.
# Never put secrets (tokens, passwords, connection strings) in any VITE_* variable.

VITE_APP_NAME=Carpool & Fleet Platform
VITE_API_BASE_URL=/api
